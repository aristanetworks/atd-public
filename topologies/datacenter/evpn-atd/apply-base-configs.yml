---

- name: Apply Base Configs for EVPN ATD
  hosts: evpn
  gather_facts: no

  tasks:
    - name: Gather Facts
      eos_facts:

    - name: Apply Base Configs
      eos_config:
        src: "./backup/{{ inventory_hostname }}-evpn-base.cfg"
        replace: config
        save_when: always

